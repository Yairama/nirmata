[package]
name = "nirmata-store"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
nirmata-core = { path = "../nirmata-core" }
rusqlite = { version = "0.37", features = ["bundled"] }
serde.workspace = true
serde_json = "1"
thesaurus = { version = "0.5.2", default-features = false, features = ["wordnet"] }
