[package]
name = "nirmata-app"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
nirmata-ai = { path = "../nirmata-ai" }
nirmata-core = { path = "../nirmata-core" }
nirmata-store = { path = "../nirmata-store" }
serde.workspace = true
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt", "time"] }
